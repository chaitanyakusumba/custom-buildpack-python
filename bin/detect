# #!/usr/bin/env bash
# BUILD_DIR=$1
# echo $BUILD_DIR
# # Exit early if app is clearly not Python.
# if [ ! -f "$BUILD_DIR/requirements.txt" ] && [ ! -f "$BUILD_DIR/setup.py" ] && [ ! -f "$BUILD_DIR/Pipfile" ]; then
#   exit 1
# fi

# echo Python

# detect

#!/bin/bash
echo "exiting with pythonapp"
exit 1
